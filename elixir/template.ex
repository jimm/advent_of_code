PUZZLENAME

defmodule YEARNAME.DAYNAME do
  def part1(_ctx, lines) do
    IO.puts(lines)
  end

  def part2(_ctx, lines) do
    IO.puts(lines)
  end

  # ================ helpers ================
end
