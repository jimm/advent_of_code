PUZZLENAME

import utils


def part1(ctx, lines=None):
    if not lines:
        lines = utils.data_file_lines(ctx)


def part2(ctx, lines=None):
    if not lines:
        lines = utils.data_file_lines(ctx)
