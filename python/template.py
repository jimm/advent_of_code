# ================ PUZZLENAME ================
# URL

from utils import *


def part1(ctx):
    lines = data_file_lines(ctx)


def part1_test(ctx):
    run_chunk_tests(
        ctx, lambda expected, lines: (expected == lines[0], lines[0])
    )


def part2(ctx):
    lines = data_file_lines(ctx)


def part2_test(ctx):
    run_chunk_tests(
        ctx, lambda expected, lines: (expected == lines[0], lines[0])
    )
